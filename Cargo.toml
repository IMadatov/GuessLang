[package]
name = "text_analyzer_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
regex = "1.10"
once_cell = "1.19"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
